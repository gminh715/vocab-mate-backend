enum UserRole {
  ADMIN
  USER

  @@map("user_role")
}

enum UserStatus {
  ACTIVE
  SUSPENDED
  DISABLED

  @@map("user_status")
}

enum CefrLevel {
  A1
  A2
  B1
  B2
  C1
  C2

  @@map("cefr_level")
}

enum ArticleStatus {
  DRAFT
  PUBLISHED
  ARCHIVED

  @@map("article_status")
}

enum AiGenerationStatus {
  PENDING
  PROCESSING
  READY
  FAILED

  @@map("ai_generation_status")
}

enum TermOrigin {
  MANUAL
  AI
  NLP

  @@map("term_origin")
}

enum TermReviewStatus {
  PENDING
  APPROVED
  REJECTED

  @@map("term_review_status")
}

enum ReadingStatus {
  READING
  COMPLETED

  @@map("reading_status")
}

enum FsrsCardState {
  NEW
  LEARNING
  REVIEW
  RELEARNING

  @@map("fsrs_card_state")
}

enum TutorSessionStatus {
  ACTIVE
  COMPLETED
  ABANDONED

  @@map("tutor_session_status")
}

enum TutorSessionItemStatus {
  PENDING
  ANSWERED
  SKIPPED

  @@map("tutor_session_item_status")
}

enum TutorQuestionType {
  MULTIPLE_CHOICE
  CONTEXTUAL_CLOZE
  TYPED_RECALL
  MICRO_LESSON_RETEST

  @@map("tutor_question_type")
}
