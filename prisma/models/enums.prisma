enum UserRole {
  ADMIN
  USER

  @@map("user_role")
}

enum UserStatus {
  ACTIVE
  SUSPENDED
  DISABLED

  @@map("user_status")
}

enum CefrLevel {
  A1
  A2
  B1
  B2
  C1
  C2

  @@map("cefr_level")
}

enum ArticleStatus {
  DRAFT
  PUBLISHED
  ARCHIVED

  @@map("article_status")
}

enum AiGenerationStatus {
  PENDING
  PROCESSING
  READY
  FAILED

  @@map("ai_generation_status")
}

enum TermOrigin {
  MANUAL
  AI
  NLP

  @@map("term_origin")
}

enum TermReviewStatus {
  PENDING
  APPROVED
  REJECTED

  @@map("term_review_status")
}

enum LearningStatus {
  NEW
  LEARNING
  REVIEWING
  MASTERED
  IGNORED

  @@map("learning_status")
}

enum ReadingStatus {
  READING
  COMPLETED

  @@map("reading_status")
}

enum LexicalUnitType {
  WORD
  PHRASE

  @@map("lexical_unit_type")
}

enum QuestionType {
  SELECT_MEANING
  SELECT_WORD
  SELECT_CORRECT_CONTEXT
  FILL_BLANK

  @@map("question_type")
}

enum ReviewQuestionGenerationSource {
  AI

  @@map("review_question_generation_source")
}

enum ReviewSessionItemStatus {
  PENDING
  COMPLETED
  SKIPPED

  @@map("review_session_item_status")
}

enum ReviewSessionStatus {
  IN_PROGRESS
  COMPLETED
  ABANDONED

  @@map("review_session_status")
}

enum ReviewGoal {
  BALANCED
  RECALL
  SPELLING
  CONTEXT

  @@map("review_goal")
}

enum ReviewSkillDimension {
  RECOGNITION
  RECALL
  SPELLING
  CONTEXT
  PRODUCTION

  @@map("review_skill_dimension")
}

enum ReviewErrorType {
  LOW_RECALL
  MEANING_CONFUSION
  CONFUSABLE_WORD
  SPELLING_ERROR
  WORD_FORM_ERROR
  COLLOCATION_ERROR
  CONTEXT_MISUNDERSTANDING
  CARELESS_ERROR
  UNKNOWN

  @@map("review_error_type")
}

enum ReviewAgentAction {
  CONTINUE
  REQUEUE_WITH_NEW_TYPE
  TEACH_AND_REQUEUE
  FLAG_FOR_FUTURE_FOCUS

  @@map("review_agent_action")
}

enum ReviewDecisionKind {
  SESSION_PLAN
  ANSWER_INTERVENTION
  SESSION_SUMMARY

  @@map("review_decision_kind")
}

enum ReviewDecisionSource {
  AI

  @@map("review_decision_source")
}
